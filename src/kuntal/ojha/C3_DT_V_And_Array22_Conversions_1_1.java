package kuntal.ojha;

public class C3_DT_V_And_Array22_Conversions_1_1 {
    public static void main(String[] args) {
        byte b;
        int i;

        // Conversion of int to byte. -10 to 10
        System.out.println("\nConversion of int to byte. -10 to 10\n");
        for (i=-10;i<10;i++)
        {
            // Conversion of int to byte.
            System.out.println("\nConversion of int to byte.");
            b=(byte) i;
            System.out.println("i and b "+ i + " int to byte " + b);
        }

        // Conversion of int to byte. -120 to 136
        System.out.println("\nConversion of int to byte. -120 to 136\n");
        for (i=-120;i<136;i++)
        {
            // Conversion of int to byte.
            System.out.println("\nConversion of int to byte.");
            b=(byte) i;
            System.out.println("i and b "+ i + " int to byte " + b);
        }

        // Conversion of int to byte. -146 to 266
        System.out.println("\nConversion of int to byte. -146 to 266\n");
        for (i=-146;i<266;i++)
        {
            // Conversion of int to byte.
            System.out.println("\nConversion of int to byte.");
            b=(byte) i;
            System.out.println("i and b "+ i + " int to byte " + b);
        }
    }
}
